public interface PermutationProcessor {
    /**
     * Операция 1: поиск групп анаграмм заданного размера в тексте.
     * @param text исходный строковый текст
     */
    void findPermutationGroups(String text);
}
